import { Heading } from '@chakra-ui/react';

const Header = () => {
    return (
        <Heading mb={6} align="center">
            Rick and Morty
        </Heading>
    );
};

export default Header;
